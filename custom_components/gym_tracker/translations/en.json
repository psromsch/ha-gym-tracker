{
  "config": {
    "step": {
      "user": {
        "title": "Gym Tracker",
        "description": "Set up Gym Tracker. This creates the workout-tracking entities; there is nothing to configure. Select **Submit** to continue."
      }
    },
    "abort": {
      "single_instance_allowed": "Gym Tracker is already configured. Only a single instance is allowed."
    }
  },
  "services": {
    "add_set": {
      "name": "Add set",
      "description": "Record a set for an exercise using its current weight and reps inputs. Both must be greater than zero or the call is a no-op.",
      "fields": {
        "base": {
          "name": "Exercise",
          "description": "The exercise base slug (e.g. bench_press)."
        }
      }
    },
    "undo_last_set": {
      "name": "Undo last set",
      "description": "Remove the most recent set. Falls back to the last set of the most recent finished session if the current session is empty.",
      "fields": {
        "base": {
          "name": "Exercise",
          "description": "The exercise base slug (e.g. bench_press)."
        }
      }
    },
    "finish_exercise": {
      "name": "Finish exercise",
      "description": "Close out the current session, prepending it to history. No-op if the current session has no sets.",
      "fields": {
        "base": {
          "name": "Exercise",
          "description": "The exercise base slug (e.g. bench_press)."
        }
      }
    },
    "delete_set": {
      "name": "Delete set",
      "description": "Remove one set from any session. Subtracts its volume from the session total and the lifetime total (both clamped at 0) and recomputes max weight. Does not adjust weekly muscle counters.",
      "fields": {
        "base": {
          "name": "Exercise",
          "description": "The exercise base slug (e.g. bench_press)."
        },
        "session_index": {
          "name": "Session index",
          "description": "-1 = current (Today) session; 0 = most recent finished (T-1); 1 = T-2; …"
        },
        "set_index": {
          "name": "Set index",
          "description": "0-based index of the set within the session (0 = first set)."
        }
      }
    },
    "delete_session": {
      "name": "Delete session",
      "description": "Remove a whole session and subtract its total from the lifetime total (clamped at 0). Does not adjust weekly muscle counters.",
      "fields": {
        "base": {
          "name": "Exercise",
          "description": "The exercise base slug (e.g. bench_press)."
        },
        "session_index": {
          "name": "Session index",
          "description": "-1 = current (Today) session; 0 = most recent finished (T-1); 1 = T-2; …"
        }
      }
    },
    "add_exercise": {
      "name": "Add exercise",
      "description": "Add a user-defined exercise and create its entities (via a config-entry reload). Built-in exercises cannot be redefined. Re-adding a previously removed base restores its stored history.",
      "fields": {
        "base": {
          "name": "Base slug",
          "description": "Lowercase entity-id stem, e.g. cable_fly. Letters, digits, underscores."
        },
        "name": {
          "name": "Display name",
          "description": "Friendly name, e.g. \"Cable Fly\"."
        },
        "focus": {
          "name": "Day focus",
          "description": "Which day-focus group the exercise lives under."
        },
        "muscles": {
          "name": "Muscle groups",
          "description": "One or more muscle groups this exercise credits per set."
        },
        "chart_min": {
          "name": "Chart minimum",
          "description": "y-axis floor for the exercise's ApexCharts card."
        }
      }
    },
    "remove_exercise": {
      "name": "Remove exercise",
      "description": "Remove a user-added exercise and its entities (via a config-entry reload). Built-in exercises cannot be removed. Stored history is kept so re-adding the same base restores it.",
      "fields": {
        "base": {
          "name": "Base slug",
          "description": "The user-added exercise's base slug."
        }
      }
    }
  }
}
